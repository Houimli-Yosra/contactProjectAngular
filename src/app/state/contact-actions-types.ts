import {Contact} from '../model/contact';

export enum ContactActionsTypes {
  GET_All_CONTACTS = '[contact] GET ALL contacts' ,
  NEW_CONTACT = '[contact] NEW contact',
  DELETE_CONTACT = '[contact] Delete contact',
  EDIT_CONTACT = '[contact] Edit contact',
  Contact_Added= '[contact] Contact Added',
  Contact_UPDATE= '[contact] Contact UPDATE',
}

